# eSM Prequal Timing Loader

This app is Windows only, since iRacing is Windows only.

You need
- Go https://go.dev/dl/
- make (https://chocolatey.org/install & `choco install make`)

Then `make build` and run the loader console app.